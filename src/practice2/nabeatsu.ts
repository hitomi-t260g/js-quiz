/**
 * [問題]
 * 引数として受け取った数字が、3の倍数であれば"あほ"を返し、
 * そうでなければ数字をそのまま返す関数nabeatsuを作成してください。
 * 例えば6を受け取ったら"あほ"を返し、7を受け取ったら7を返してください。
 */
export const nabeatsu = (num: number): string | number => {
  // TODO
  // 下記のreturnは消して、自分の関数を作ってください。
  return 0;
};
